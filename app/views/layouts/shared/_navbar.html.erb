<nav class="navbar navbar-expand-lg navbar-light bg-success bg-opacity-50 px-sm-5">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Hola <b><%= current_user ? current_user.fullname : "Usuario no logeado" %></b>! </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link <%= 'active' if params[:controller] == 'pages' %>" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item ">
          <a class="nav-link <%= 'active' if params[:controller] == 'tasks' %>" href=<%=tasks_path%>>Tasks</a>
        </li>
      </ul>
      
      <% if user_signed_in? %>
        <%= button_to "Cerrar sesión", destroy_user_session_path, method: :delete, class: 'btn btn-glass bg-light' %>
      <% else %>
        <%= link_to "Iniciar sesión", new_user_session_path, class: 'btn btn-success text-dark' %>
      <% end %>
    </div>
  </div>
</nav>
