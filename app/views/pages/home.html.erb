<div class="container">
  <h1> <strong> Bienvenido! </strong> </h1>
  <% if current_user && current_user.photo_profile.key %>
    <%= image_tag current_user.photo_profile.variant(resize_to_limit: [400, 400]) %>
  <% end %>
</div>
