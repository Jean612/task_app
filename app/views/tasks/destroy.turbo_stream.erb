<% if @view == 'list' %>
  <%= turbo_stream.replace "tasks_list", partial: 'task_card_view', locals: { tasks: @tasks } %>
<% elsif @view == "dashboard" %>
  <%= turbo_stream.remove "task_#{@task.id}_dashboard" %>  
<% end %>

<%#<%= turbo_stream.update "flash", partial:"layouts/shared/flash_messages" %>
